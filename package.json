{
  "name": "cv",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive single-file CV — behavioural test suite (Playwright)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
