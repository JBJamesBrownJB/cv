# Private working material — tailored to a specific opportunity, never publish.
research/
ideas.md

# Build artifact — regenerated from index.html by CI (npm run pdf) on every deploy.
# Tracking it only lets a stale copy drift out of sync with the live site.
JB_CV.pdf

# Scratch / preview artifacts
review/
review2/
review3/
.playwright-mcp/
*.tmp

# Test tooling (dev-only)
node_modules/
test-results/
playwright-report/
blob-report/
/graph-schroders.png

# OS noise
Thumbs.db
.DS_Store

# local dev server
serve.js
